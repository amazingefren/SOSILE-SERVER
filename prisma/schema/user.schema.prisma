// https://ftpdocs.broadcom.com/cadocs/0/e002961e.pdf
model User {
  id        Int     @id   @default(autoincrement())
  username  String  @unique
  email     String  @unique
  
  role      Role    @default(USER)

  // User Relations
  posts     Post[]
  profile   UserProfile?

  // User -> Liked Posts
  // User -> Replied Posts
  likes     Post[]    @relation(name: "userLiked")
  replies   Post[]   @relation(name: "userReply")
}

model UserProfile {
  id          Int   @id @default(autoincrement())
  biography   String
  user        User  @relation(fields: [userId], references: [id])
  userId      Int
}

enum Role {
  USER
  ADMIN
}
